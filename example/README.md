# upsync_example

Ejemplo minimo para probar `upsync` dentro del mismo paquete.

La app no trae un manifest real por defecto. Abrela, coloca la URL de tu
manifest y ejecuta la revision manual para ver el flujo del plugin.
